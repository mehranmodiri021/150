# راهنمای بیلد خودکار در GitHub Actions و انتشار در کافه‌بازار

پروژه برای بیلد کاملاً خودکار در GitHub Actions بدون هیچ نیازی به تغییرات دستی پیکربندی شده است.

---

### فرمت‌های خروجی بیلد (Artifacts):
هنگام اجرای ورک‌فلو یا هر Push به ریپازیتوری در گیت‌هاب، فایل‌های زیر به عنوان **Artifact** آماده دانلود خواهند بود:

1. **نسخه APK (`ArenaClash-APK`):**
   - فایل نصبی استاندارد اندروید برای تست مستقیم روی دستگاه‌ها و مارکت‌های اندرویدی.
2. **نسخه AAB (`ArenaClash-AAB`):**
   - بسته Android App Bundle بهینه‌سازی‌شده برای بارگذاری در پنل‌های توسعه‌دهندگان مدرن.
3. **نسخه BIN مخصوص کافه‌بازار (`ArenaClash-CafeBazaar-BIN`):**
   - فایل `arena_clash_bazaar.bin` طبق ساختار و فرمت مورد پذیرش پنل توسعه‌دهندگان کافه‌بازار برای پکیج‌های بیلد شده.

---

### تنظیمات متغیرهای محرمانه (GitHub Secrets - اختیاری):
در صورت تمایل به امضای رسمی (Release Keystore) و تنظیم کلیدهای تپسل و کافه‌بازار، مقادیر زیر را در بخش **Settings > Secrets and variables > Actions** مخزن گیت‌هاب وارد کنید:

- `TAPSELL_APP_KEY`: کلید برنامه تپسل پلاس
- `TAPSELL_REWARDED_ZONE_ID`: شناسه زون تبلیغات جایزه‌ای
- `TAPSELL_BANNER_ZONE_ID`: شناسه زون بنر
- `TAPSELL_INTERSTITIAL_ZONE_ID`: شناسه زون بین‌برنامه‌ای
- `BAZAAR_PUBLIC_KEY`: کلید عمومی RSA پرداخت درون‌برنامه‌ای کافه‌بازار
- `RELEASE_KEYSTORE_BASE64`: فایل کی‌استور خروجی به صورت Base64
- `RELEASE_KEYSTORE_PASSWORD`: رمز عبور Keystore
- `RELEASE_KEY_ALIAS`: نام مستعار کلید (Alias)
- `RELEASE_KEY_PASSWORD`: رمز عبور کلید

> **نکته:** در صورتی که این متغیرها در گیت‌هاب تنظیم نشوند، اسکریپت بیلد به صورت خودکار بدون هیچ‌گونه خطا از مقادیر پیش‌فرض و امضای خودکار پروژه استفاده کرده و بیلد با موفقیت کامل به اتمام می‌رسد.
